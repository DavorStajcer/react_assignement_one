interface Note {
  title: string;
  content: string;
}

/* class Note {
  constructor(note: Note) {
    this.title = note.title;
    this.content = note.content;
  }

  isValid(): boolean {
    return this.title !== "" && this.content !== "";
  }
}
 */
